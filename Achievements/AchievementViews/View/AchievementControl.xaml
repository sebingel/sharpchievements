<UserControl x:Class="sebingel.sharpchievements.AchievementViews.View.AchievementControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Height="90" HorizontalAlignment="Stretch"
             DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Grid>

        <Border BorderThickness="1" Background="#4b4b4b" BorderBrush="Black">
            <StackPanel Orientation="Horizontal">
                <Image Height="80" Width="80" Source="{Binding ImagePath}" Visibility="{Binding ImageVisibility}"/>
                <StackPanel>
                    <TextBlock Margin="5" FontWeight="Bold" Text="{Binding Titel}" Foreground="#B0B0B0"/>
                    <TextBlock Margin="5,0" Text="{Binding Description}" Foreground="#c9c9c9"/>
                </StackPanel>
            </StackPanel>
        </Border>

    </Grid>

</UserControl>
